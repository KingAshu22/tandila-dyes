import ManagerRegisterForm from "@/components/ManagerForm";
import React from "react";

const CreateClient = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <ManagerRegisterForm />
        </div>
    );
};

export default CreateClient;